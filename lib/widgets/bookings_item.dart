import 'package:flutter/material.dart';

class BookingsItem extends StatelessWidget {
  const BookingsItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}